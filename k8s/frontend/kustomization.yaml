apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - deployment.yaml
  - service.yaml
  - service-account.yaml

commonLabels:
  app.kubernetes.io/name: flask-nginx
  app.kubernetes.io/component: fronetned
  app.kubernetes.io/part-of: mykola 
  environment: dev 
images:
  - name: mykaua/flask-nginx
    newTag: v0.0.3
