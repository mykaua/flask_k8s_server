---
apiVersion: v1
kind: Service
metadata:
  name: flask-nginx
  annotations:
    service.kubernetes.io/topology-aware-hints: auto
spec:
  type: ClusterIP
  ports:
    - port: 8080 
      targetPort: http
      protocol: TCP
      name: http
  selector:
